import 'package:flutter/material.dart';

class LullabyPage extends StatelessWidget {
  const LullabyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Ninniler')));
  }
}
